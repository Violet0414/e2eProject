**一、菜单**

1. 菜单名称是否正确
2. 菜单图标
3. 菜单权限：有权限显示菜单，没有权限则不显示菜单

**二、列表查询**

1. 列表标题是否正确
2. 查询条件显示是否正确
3. 查询条件的默认提示
4. 选择类的查询条件，下拉项是否正确
5. 日期类的查询条件，日期控件是否正确
6. 时间类的查询条件，是否可正确选择时间
7. 树形结构的查询条件，可以展开和收起，层级和选项显示正确
8. 省市区级联选择的查询条件，可以根据选择的省过滤市，根据市过滤区，根据区过滤街道等
9. 单个查询条件的查询结果是否正确
10. 多个条件组合查询条件是否正确
11. 查询结果为空
12. 查询结果又多页，翻页后查询结果显示正确
13. 重置查询条件可以正确将所有查询条件重置为默认情况
14. 展开、收起查询条件可以正常操作
15. 按钮名称显示正确
16. 先翻页，翻页后查询数据。页码重置为第一页查询结果正确

**三、列表数据**

1. 列表的表头字段显示正确
2. 列表字段值显示正确：新增后根据新增时输入的值进行对比；编辑后根据编辑后输入的值进行对比；没有新增、编辑操作的根据接口响应的字段值作对比
3. 查询后列表数据显示正确
4. 删除数据后，列表不显示删除的数据

**四、列表翻页**

1. 显示翻页控件
2. 可以点击页码进行翻页
3. 可以输入页码进行翻页
4. 可以点击上一页、下一页按钮翻页
5. 可以点击第一页（若有按钮）、最后一页（若有按钮）翻页
6. 可以选择设置每页显示的条数，设置后每页显示的数据条数正确
7. 翻页后数据显示正确

五、列表操作
1. 操作按钮名称
2. 操作按钮按照数据状态显示（若测试用例或需求文档下有限制，若没有限制则不测试）
3. 点击按钮可以跳转到对应的页面
4. 删除操作：1）需要有确认提示弹窗；2)删除后列表不显示所删除的数据；3）测试脚本只能删除自己新增的数据
5. 批量删除：1）需要有确认提示弹窗；2)删除后列表不显示所删除的数据；3）没有选择任何数据点击删除，需进行提示；4）查询后全选删除只能删除查询的数据；5）只能选择自己新增的数据进行删除
6. 其他批量操作：1）没有选择数据，点击按钮需要进行提示；2）选择后对应操作所选择的数据

**六、新增表单**

1. 页面标题显示
2. 每个字段的默认提示
3. 页面字段显示
4. 单文本类型的字段：1）规范数据输入测试；2）特殊字符输入测试；3）最大长度输入测试；4）大于最大长度输入测试
5. 多行文本字段：1）规范数据输入测试；2）特殊字符输入测试；3）最大长度输入测试；4）大于最大长度输入测试；5）换行输入测试
6. 下拉选择字段：1）下拉选项显示正确；2）可以正常选择选项；3）可以切换选项
7. 多选字段：1）选项显示正确；2）可以选择多个选项；3）可以选择一个选项；4）可以取消选择
8. 日期字段：1）可选的日期范围需要符合日常生活逻辑；2）可以切换年、月、日选择日期；3）可以正常选择日期
9. 时间字段：1）可以选择到时间点或时间段
10. 金额字段：1）小数位限制；2）整数位长度控制；3）只能输入数字；4）不能输入特殊符号；5）不能输入文字、英文等
11. 身份证号码字段：1）长度限制；2）身份证格式校验
12. 手机号字段：1）长度限制；2）格式验证；3）若支持座机需要可以输入座机号
13. 地图选址字段：1）点击可以打开地图弹窗；2）可以点击地图选择地址；3）可以输入地址查询再进行选择；3）选择后回填到字段值下，关闭地图弹窗
14. 附件：1）附件个数限制；2）附件类型限制；3）上传附件后可以删除重新上传；4）附件大小
15. 图片：1）图片个数限制；2）图片类型限制；3）上传图片后可以删除重新上传；4）图片大小
16. 非必填项验证：1）不填写可以保存；2）填写可以保存
17. 必填项严重：1）必填项标识；2）不填写保存需进行提示；3）填写后可以保存
18. 新增页面的关闭按钮点击后关闭新增页面
19. 新增页面的保存或者提交按钮，点击后可以正常校验信息或者提交数据
20. 新增页面的取消按钮，点击后可以关闭新增页面
21. 新增数据保存成功后，列表显示该数据
22. 新增数据保存成功后，列表的每个字段值显示正确

**六、编辑表单**

1. 新增页面的所有的验证点编辑页面一样的验证
2. 编辑页面：每个字段是否正确回填，需要回填的字段值需要和新增保存的对应
3. 编辑页面：清空非必填字段保存，是否可以保存成功。且保存后对应列表、详情等地方字段值是否显示空
4. 关闭页面按钮：关闭页面数据不更新
5. 保存按钮，保存后数据按照编辑的数据值进行更新
6. 取消按钮：取消后数据不更新
7. 关闭页面附件正常回显

**七、详情表单**

1. 页面标题
2. 详情显示的字段名称正确
3. 每个字段对应的字段值显示正确：字段值需要和新增、编辑的一致
4. 图片字段，点击图片可以进行预览
5. 附件字段：文件可以进行下载，下载后可以正常打开查看
6. 详情有子TAB的每个TAB可以正常切换，对应的内容显示正确
7. 详情：返回或者关闭按钮，点击后关闭详情
8. 空字段值显示
9. 字段值较长显示效果

**八、整个过程监测接口的传参和响应结果，可以分析出bug属于前端还是后端**