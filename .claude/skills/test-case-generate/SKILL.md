---
name: test-case-generate
description: 用例生成 - 将测试点清单转化为结构化测试用例，含ID/标题/前置/步骤/数据/预期
triggers:
  - "用例生成"
  - "生成用例"
  - "写用例"
---

# 用例生成

将测试点清单转化为结构化测试用例，每条用例包含完整的执行信息。

## 输入

- 测试点清单（通常由"测试点提取"技能产出，经"测试点评审"通过）
- 可选：业务规则补充说明
- **必读参考文件**：`./referenceDocument/testPointsRequirement.md`，生成测试数据时必须参照此文件中的字段规则（金额小数位限制、身份证格式、手机号长度等）

## 用例结构

每条测试用例必须包含以下字段：

| 字段 | 说明 |
|------|------|
| 用例ID | 格式：TC-{模块缩写}-{3位序号}，如 TC-LOGIN-001 |
| 标题 | 简洁描述用例目的，不超过30字 |
| 优先级 | 继承自关联测试点的优先级（P0/P1/P2） |
| 关联测试点 | 对应的测试点编号 |
| 前置条件 | 执行本用例前必须满足的条件 |
| 步骤 | 操作步骤，编号列出 |
| 测试数据 | 每个步骤使用的具体输入数据 |
| 预期结果 | 每个步骤对应的期望输出/状态 |

## 处理规则

1. **一对一映射**：一个测试点生成一条主用例；如果测试点涉及多种数据组合，可拆为多条用例
2. **步骤粒度**：每个步骤是一个明确的用户操作或系统行为，可独立验证
3. **测试数据具体化**：
   - 不写"有效手机号"，写"13800138000"
   - 不写"超长文本"，写"256个汉字的字符串：测试测试…"
   - 边界值明确给出具体数值
   - **参照 referenceDocument/testPointsRequirement.md**：金额字段按小数位限制和整数位长度生成数据，身份证按格式校验规则生成，手机号按长度和格式规则生成，附件/图片按个数、类型、大小限制生成
4. **预期结果可判定**：预期结果必须是可观察、可判定的，避免"正常显示"这类模糊表述
5. **数据驱动场景**：如果多个用例仅数据不同（如等价类），合并为一条用例，在数据字段用表格列出多组数据

## 输出格式（参照 referenceDocument/用例模板.xlsx）

```markdown
## 测试用例

**关联需求**：{需求名称}
**生成日期**：{当前日期}

---

| 用例编号 | 所属产品 | 所属模块 | 用例名称 | 前置条件 | 步骤 | 预期 |
|----------|----------|----------|----------|----------|------|------|
| TC-{模块}-{3位序号} | {产品名称} | {模块路径} | 【{子模块}】{验证点} | {前置条件1}<br>{前置条件2} | {步骤1}<br>{步骤2} | {预期1}<br>{预期2} |

---
```

## 注意事项

- 用例ID全局唯一，不与已有用例冲突
- 如果测试点描述过于模糊无法生成具体用例，列出疑问向用户确认
- 生成后统计用例总数和模块分布

## 持久化

- 生成结果必须同时写入文件 `./output/{当前日期}/测试用例.md`（如目录不存在则先创建，"当前日期"格式为YYYY-MM-DD，如2026-05-15）
- 文件已存在时询问用户是否覆盖
